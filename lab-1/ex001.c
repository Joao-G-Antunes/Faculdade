#include <stdio.h>

int main(){
      float n1, n2, media;
      printf("Digite as duas notas do aluno:");
      scanf("%f %f", &n1, &n2);
      media = (n1 + n2)/2;
      printf("A media do aluno e: %.2f", media);
 return 0;

}	